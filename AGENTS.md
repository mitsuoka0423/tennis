# AGENTS.md

## 方針

本リポジトリでは以下を保証する方針とする。

1. 中断可能性
2. 一貫性
3. メンテナンス容易性

## ルール

方針を満たすため、以下のルールを必ず守ること。

1. 作業ログを残すこと(方針1)
   - ./Logs/AGENTS.md を参照すること
2. コミット単位で以下の一貫性を保証すること(方針2)
   - ドキュメント間
   - ドキュメントと実装ファイル
   - 実装ファイル間
3. ファイルに記載する内容は以下とする(方針3)
   - コード: How
   - テストコード: What
   - コミットログ: Why
   - コードコメント: Why not
4. 製品・機能の呼称は公式の正式名称を使うこと(方針2)
   - Apple 製品の部位・機能は Apple 公式の日本語表記に従う
   - UI 文言・ドキュメント・コードコメントすべてに適用する
5. コードを変更したら必ずビルドが通ることを確認すること(方針2,3)
   - CLI (`xcodebuild`) と、Xcode MCP が使える場合は Xcode 経由（`BuildProject`）の両方を確認する
   - 両者の結果が食い違う場合は Xcode エディタのバッファ同期不良を疑い、
     `XcodeWrite` → `XcodeRefreshCodeIssuesInFile` → `BuildProject` の順で解消してから完了とする
   - Xcode MCP が無い環境では、ユーザーに Xcode でのビルド確認を依頼する

## ガイドライン

### コードコメントの書き方（ルール3）

各ファイル種別の記載内容の具体例を以下に示す。

| ファイル種別 | 記載内容 | 悪い例 | 良い例 |
|---|---|---|---|
| コード（実装） | How | `// MainActorのプロパティにTaskでアクセス` | コードそのもので表現する |
| テストコード | What | `// テストする` | `// 正常系: 800Hz指定時にサンプルが1秒あたり800個届くこと` |
| コミットログ | Why | `fix: build error` | `fix: CMBatchedSensorManagerはプロパティではなくAsyncSequenceでデータを提供するため` |
| コードコメント | Why not | `// バッファリング書き出し` | `// CoreDataではなくCSVを選択: Phase2でWCSession.transferFileに直接渡せるためフォーマット変換が不要` |

Why not コメントの書き方:
- 「なぜ別の実装を選ばなかったか」を書く
- 「なぜこの実装にしたか」のトレードオフを書く
- コードを読めばわかること（How）は書かない

### 正式名称の例（ルール4）

| 俗称・避ける表記 | 正式名称 |
|---|---|
| リューズ、クラウン | Digital Crown |
| 横のボタン | サイドボタン |
| アップルウォッチ | Apple Watch |

- 迷った場合は Apple Japan のユーザガイド・サポートページの表記を確認する
- ユーザーから俗称で指示があった場合も、正式名称を提示して確認の上で正式名称を使う