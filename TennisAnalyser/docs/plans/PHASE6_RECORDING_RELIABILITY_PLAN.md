# F-I7 録画継続性 実装計画（Phase 6）

**状態: W1〜W4 完了 / W6 完了（T14・T15・T16a〜e・T17）**（2026-07-25）
**仕様**: [docs/RECORDING_RELIABILITY_SPEC.md](../RECORDING_RELIABILITY_SPEC.md)
**契機**: 2026-07-21 実機検証で 325スイング中クリップ1件のみ生成される不具合を確認

---

## 方針の変更（2026-07-25）

データ収集を**連続記録＋事後タグ付け**へ移行する（仕様書 6章）。

動画は既に連続録画されており、捨てているのはセンサー側だけである。
新たに要るのは「センサーの全区間保存」と「アノテーションUI」の2点にとどまる。

W1・W2（録画継続・セグメント分割）は**変更なく必要**。動画が一次データになるため
重要度はむしろ上がる。以下が変わる。

| タスク | 変更 |
|---|---|
| T8（削除の安全化） | 「クリップ生成まで保持」→ **既定で保持・自動削除しない**へ反転 |
| T11（閾値） | 分析済み。実装変更は**不要**（事後調整のパラメータになるため） |
| T12（re-arm） | **消滅**。撮影時に検知しないため不感時間が存在しない |
| T13（ラベル付与） | W6 のアノテーションUIへ発展 |

新規に W6（連続記録とアノテーション）を追加する。着手は W2 完了後。

---

## 方針（当初）

原因は「自動ロックで録画が止まり、再開経路が無い」ことだが、
**特定の中断要因だけを塞ぐのではなく、中断一般から復帰できる構造にする**。

理由は2つ。

1. 今回の中断要因は「自動ロックである」と断定できていない。ログが1行も残っておらず、
   ファイル配置からの逆算で「開始4〜29秒後に停止」までしか絞れていない。
2. カメラ撮影を継続できるバックグラウンドモードは iOS に無いため、
   着信等による中断はどのみち発生する。防ぐのではなく復帰させる必要がある。

あわせて **W0（観測性）を最初に片付ける**。実機検証は1時間の実練習が必要で再現コストが高く、
次の検証を無駄にしないため。

---

## Wave 構成

各 Wave は独立してビルド・コミット可能。中断時は Wave 境界で止めるのが望ましい。

| Wave | 内容 | 依存 |
|---|---|---|
| W0 | 観測性の確保（os.Logger + 診断記録の器） | なし |
| W1 | 録画継続（自動ロック抑止 + 中断復帰） | W0 |
| W2 | セグメント分割録画とデータモデル刷新 | W1 |
| W3 | セグメント保持への切り替え | W2 |
| W4 | 診断記録のUI表示と共有 | W2 |
| W5 | 検出精度の分析（T11 完了 / T12 消滅 / T13 は W6 へ） | — |
| W6 | 連続記録とアノテーション（方針変更により新設） | W2 |

---

## タスク分解

### W0: 観測性の確保

- [x] **T1: `os.Logger` 基盤の導入** ✅ 2026-07-25
  - `AppLog` を iOS/Watch 各ターゲットに新設。サブシステム `com.spleeing.TennisAnalyser`
    - iOS カテゴリ: `recording` / `clip` / `transfer` / `store` / `session`
    - Watch カテゴリ: `motion` / `swing` / `transfer`
  - `print` を全35箇所置換（iOS 16・Watch 19）。残存 0 件
  - 個人情報を含まないため `privacy: .public` を明示（既定の `.private` だと
    実機ログで値がマスクされ、今回のような事後調査で使えない）
  - `VideoStore.extractClipIfNeeded` の**無言の早期 return 全てにログを追加**。
    今回の不具合を隠していた箇所であり、T1 の主目的
  - ビルド確認: iOS ✅ / Watch ✅（CLI `xcodebuild`）
  - ⚠️ 未実施: ユニットテスト（ディスク容量不足で Simulator が起動せず）、
    Xcode GUI ビルド（Xcode MCP 切断中）、Console.app での実機確認

  > Why not 共有ファイル1つ: 同期グループ構成のためターゲットごとにフォルダが対応する。
  > 共有には project.pbxproj の手編集が必要でリスクが見合わないため、各ターゲットに配置した。

- [x] **T2: 診断記録の器を作る** ✅ 2026-07-25
  - `DiagnosticEvent`（Domain）: 出来事1件。追記のみで記録する
  - `SessionDiagnostics`（Domain）: 出来事の列から集計を**導出**する純粋関数
  - `DiagnosticsStore`（Infrastructure）: `Documents/diagnostics/{sessionId}.jsonl` へ追記
  - 配線済み: セッション開始/終了（`PhoneSessionManager`）、
    セグメント開始/終了と終了理由（`PracticeVideoRecorder`）、
    クリップ生成の成否と理由（`VideoStore`）
  - 中断イベント（`.interrupted` / `.interruptionEnded`）は型のみ用意。記録は T4 で配線する
  - テスト: 集計ロジック 8件 ✅（2026-07-21 の実機不具合を再現する回帰テストを含む）
  - ビルド ✅ / テスト ✅（CLI）。⚠️ Xcode GUI ビルドは未確認（Xcode MCP 切断中）

  > Why not `.json` で配列を保持: 配列を保つには読み込み→追加→全体書き戻しが必要で、
  > 書き戻し中に落ちるとそれまでの記録ごと失われる。診断が最も必要なのは異常終了時であり、
  > その状況で失われる方式は採れない。1行1イベントの JSON Lines に変更した（計画時は `.json`）。

  > Why not 集計値を直接保存: 上書き保存では最後の書き込みしか残らない。
  > 出来事を追記し集計は読み出し時に導出すれば、途中で落ちてもそこまでの経過が残る。

### W1: 録画継続

- [x] **T3: 自動ロックの抑止** ✅ 2026-07-25
  - `startRecording` で `isIdleTimerDisabled = true`、`didFinishRecordingTo` で `false`
  - 戻す処理は `guard` より前に置いた。以降の早期 return でも抑止が残らないようにするため
  - `deinit` でも戻す。ただし `deinit` は nonisolated で @MainActor メソッドを呼べないため
    `Task { @MainActor in }` 経由で `UIApplication` に触る
  - ビルド ✅（CLI）。⚠️ 実機での効果確認は T4 と合わせて実施する

- [x] **T4: 中断検知と自動復帰** ✅ 2026-07-25
  - 論理状態 `isSessionActive`（セッション継続中か）と物理状態 `isRecording` を分離
  - `beginSession` / `endSession` を公開 API とし、`startNextSegment` を内部化
  - 中断の入口: `willResignActive` / `AVCaptureSessionWasInterrupted`
  - 復帰の契機: `didBecomeActive` / `AVCaptureSessionInterruptionEnded`
  - 復帰判定は `resumeIfNeeded` に集約。「論理が有効かつ物理が停止」だけで判断するため、
    どの経路から呼ばれても結果が同じで二重開始も起きない
  - 中断中は `AVCaptureSession` 自体も停止するため、再開前に `startRunning()` し直す
  - 中断・復帰を診断記録へ配線（T2 で型のみ用意していた分）
  - ⚠️ **実機での中断→復帰確認は未実施**。W2 完了後に必ず行うこと

### W2: セグメント分割録画

> **2026-07-25: T5 を T4 より先に実施する**（当初は T3→T4→T5 の順）。
> T4 の「中断からの復帰」は再開時に新しいファイルへ書き出す必要があるが
> （`.mov` は追記できない）、現行の `newSourceRecordingURL` は
> `{sessionId}.mov` の固定パスを返すため、再開すると中断前の映像を上書きして消す。
> `PracticeVideo` も `startedAt`/`endedAt` を1組しか持てず、1セッション＝複数録画を
> 表現できない。T4 の復帰機構と T7 のセグメント回転は同一の機構であり、
> 器（T5）を先に作らなければ T4 は正しく実装できない。

- [x] **T5: データモデルの刷新** ✅ 2026-07-25（型と解決ロジックのみ。切り替えは T6）
  - `RecordingSession` + `RecordingSegment` を新設（仕様書 4章）
  - `resolve(_:)` が `detectedAt` からセグメントと内部オフセットを返す。
    中断区間・録画範囲外・終了時刻未確定のセグメントはいずれも nil
  - `nextSegmentIndex` は件数ではなく既存の最大値+1。途中のセグメントを削除しても
    番号が衝突しないようにするため
  - テスト14件 ✅（解決・番号採番・集計・JSON往復）
  - **`PracticeVideo` は残置**。同時に置換するとビルドが通らない期間が生じるため、
    切り替えと削除は T6 で行う（AGENTS.md 方針1: 中断可能性）
  - 旧形式ファイルの起動時削除は T6 で実施する（切り替えと同時でなければ意味がないため）

- [x] **T6: `VideoStore` のセグメント対応（`PracticeVideo` の撤去を含む）** ✅ 2026-07-25
  - `sources: [PracticeVideo]` を `RecordingSession` ベースへ置換し、`PracticeVideo` を削除
  - `saveSourceMetadata` を manifest.json の読み書きへ変更
  - 旧形式（`{sessionId}.mov` + `.json`）が残っていれば起動時に削除。移行処理は作らない
  - `PracticeVideoTests` は `RecordingSessionTests` に置き換わるため削除
  - `extractClipIfNeeded` を「`detectedAt` を含むセグメントを検索 → 該当セグメント内の
    オフセットで切り出し」に変更
  - セグメント境界に落ちたスイングは生成不可として診断記録へ記録
  - 既存の `videoComposition(withPropertiesOf:)` による向き保持は維持する
    （F-I6 で解決済みのため壊さないこと）

- [x] **T7: 最大セグメント長** ✅ 2026-07-25
  - `maxRecordedDuration` を 10分に設定
  - 到達時は `AVError.maximumDurationReached` で通知されるため、失敗ではなく
    正常終了（`.maxDuration`）として扱い、間を空けずに次セグメントを開始する
  - 継ぎ目の欠落は診断記録の `segmentEnded` / `segmentStarted` の時刻差として残る

### W3: 中間データ削除の安全化

- [x] **T8: セグメントを保持する** ✅ 2026-07-25（T6 で実施）
  - `scheduleSourceCleanup` の無条件削除を**廃止し、自動削除そのものを行わない**
  - 猶予後の処理は未生成クリップの再試行のみに変更（`scheduleRemainingClips`）
  - 削除 API は `deleteSession` としてユーザー操作用にのみ残した
  - 削除はユーザーの明示的な操作に限る（仕様書 F-I7-4）
  - ~~保持上限7日~~ は撤回。ラベル付けは練習後にまとめて行うため、
    期限での自動削除は学習データの素材ごと失うことになる
  - 空き容量の逼迫時は削除せず警告する（W6-T17）

- [x] **T9: クリップ生成の再試行** ✅ 2026-07-25
  - 診断画面のセッション詳細に「未生成の N 件を再生成」を配置（T10 と同一画面）
  - セグメントを保持するようになったため、何度でも生成し直せる

### W4: 診断のUI

- [x] **T10: セッション診断画面** ✅ 2026-07-25
  - 3つ目のタブとして独立させた。診断は「録画が動いたか」を見る画面であり、
    スイングが1件も無い場合こそ必要になる。一覧の下位に置くと最も見たい状況で辿り着けない
  - カバー率・録画時間・セグメント数・中断回数・終了理由の内訳・クリップ成否と理由を表示
  - カバー率が 95% 未満なら赤字で強調（2026-07-21 は 0.6% に相当）
  - `ShareLink` で診断内容を共有
  - ⚠️ 未実装: セッション終了時のカバー率警告（画面側で可視化したため優先度を下げた）

### W5: 検出閾値の再評価（独立）

- [x] **T11: 保全データでの閾値分析** ✅ 2026-07-25（分析のみ。実装変更は保留）
  - 対象: 2026-07-21 セッションの 325件（下記「保全データ」参照）
  - 成果物: `~/Documents/TennisAnalyser-FieldData/2026-07-21/threshold_analysis.{py,md}`
  - **結論: 閾値 3.0g は据え置き。上げる根拠が得られなかった**

  前提が2つ誤っていた。

  1. `Swing.peakAcceleration` は**トリガ値ではなく4秒窓全体の最大値**
     （`samples.map(\.accelerationMagnitude).max()`）。実際のトリガ値は中央値 3.41g。
     窓が隣接スイングのピークを拾うため、ピークが Impact から 200ms 超ずれる窓が 116件（35.7%）
  2. 4.0g へ上げたときの脱落は「49件（15.1%）」ではなく **99件（30.5%）**（Impact±150ms で判定）

  低g帯（3〜4g）は誤検知ではない。波形は鈍い（立ち上がり 224 vs 710 g/s）が、
  ジャイロピーク中央値 525 deg/s はラケット回転として十分大きく、
  **加速度あたりの回転はむしろ低g帯が1.48倍大きい**（非スイング動作なら逆になる）。
  「弱い・回転が乏しい・鈍い」を全て満たすのは 13件（4.0%）のみ。

  | 閾値 | 残存（Impact±150ms 判定） |
  |---:|---:|
  | 3.0g | 325 (100%) |
  | 4.0g | 226 (69.5%) |
  | 5.0g | 173 (53.2%) |
  | 6.0g | 135 (41.5%) |

  絞るなら閾値ではなく **3.0g + ジャイロゲート**（Impact±100ms で 300 deg/s 以上）が筋が良い。
  除去25件で済み、8g以上の帯域は除外0件＝本物の強打を傷つけない。

  > **ラベルが全件空欄**（328ファイル・約26万行すべて）のため precision/recall は算出不能。
  > 上記はすべて分布の質的比較にとどまる。閾値を**下げた**場合の増加件数も、
  > 3g未満のセンサー値が保存されていないため算出できない。
  > 付随: 43窓で加速度計が飽和（±15.9g到達）しており 12g超の絶対値は不正確な可能性。

- [x] ~~**T12: re-arm の見直し**~~ **消滅**（2026-07-25 の方針変更により）
  - 撮影時に検知しなくなるため不感時間そのものが存在しなくなる
  - 以下は W6 のオフライン検知器を設計する際の入力として残す


  - 窓の重なりからセッションの35.1%（1196秒）で連続ストリームを復元したところ、
    その区間の 3g 超イベント578件に対し発行は325件。**242件（42%）が未発行**
  - 未発行は100%が直近検出から2.0秒以内で、`rearmAfterTs = windowEnd` の作用と完全に一致
  - うち0.6秒以上離れて残響では説明しにくいものが122件、**10g超の明らかな打球が4件まるごと欠落**
  - ラリー中の連続ショットが構造的に取りこぼされている
  - ⚠️ 被覆35%は検出周辺±2秒に偏り静止区間を含まないため、42%をセッション全体へ外挿はできない
  - **T13 の後に着手する**。ラベルが無ければ「不感時間を縮めた結果が良くなったか」を判定できず、
    取りこぼしを減らした代わりに誤検知を増やしただけ、という結果を検出できない
  - **着手前にユーザーへ提示して合意を取ること**（T11 と同様）

- [ ] **T13: 実データへのラベル付与（W6-T16 へ発展）**
  - 保全データ328件すべてで `ShotClass` が空欄であり、精度を測る土台が無い
  - 既存の付与手段（`ContentView.applyBulkTag` / `SwingDetailView`）は
    per-swing レコードが前提。連続記録ではタイムライン上で操作するUIが要る（W6-T16）
  - ラベルが空欄なのは**録画不具合の二次被害**。波形だけでは球種を判別できず、
    判別にはクリップが要るが 325件中1件しか生成されなかった

---

### W6: 連続記録とアノテーション（方針変更により新設・W2 完了後）

**仕様**: [docs/ANNOTATION_SPEC.md](../ANNOTATION_SPEC.md)（F-I8。2026-07-25 定義済み）

> 支配的な制約は**スループット**。1時間の練習で約325件が生成されるため、
> 1件あたり5秒を超えると練習時間と同等のタグ付け時間が必要になり運用が破綻する。
> W6 の設計判断はすべてこれに従属する。

- [x] **T14: センサーの全区間保存** ✅ 2026-07-25
  - Watch: `ContinuousSensorRepository`（Domain）+ `ContinuousSensorRepositoryImpl`（Infrastructure）
    - 保存先 `Documents/continuous/{sessionId}/{chunkIndex}.csv`。5分（60,000サンプル）でチャンク分割
    - 列構成はスイング単位CSVと同一（ShotClass 列のみ無い）。iPhone 側の
      `SwingCSVParser.parseSamples` をそのまま流用できるため
    - ヘッダーに時刻アンカー（`AnchorSensorMs` / `AnchorWallClock`）を持たせた。
      センサータイムスタンプは Watch 起動からの経過時間であり、動画（壁時計）と
      対応付ける基準がファイル内に無いと事後の突き合わせができない
  - 転送: `retryPending` の経路に載せる（セッション終了時・アクティベート時のみ）。
    セッション中に送るとスイング転送と帯域を奪い合うため
  - iOS: `ContinuousChunk` / `ContinuousChunkParser`（Domain）+ `ContinuousSensorStore`
    （Infrastructure）。`PhoneSessionManager` が metadata の `type` で取り込み先を振り分ける
  - **既存の per-swing CSV と併存**（両方式のデータが1回の実機検証で同時に採れる）
  - テスト: Watch 7件 ✅ / iOS 5件 ✅（CLI）
  - ⚠️ Xcode GUI ビルドは未確認（Xcode MCP が無い環境。AGENTS.md ルール5）

  > Why not 1セッション1ファイル: 1時間で約46MB になり、転送が途中で失敗すると全量を
  > 送り直すことになる。チャンク単位なら未転送分だけを再送できる。

  > Why not FileHandle を開いたまま保持する: 追記のたびに開閉すると1秒1回のコストで済み、
  > 異常終了時もそこまでの記録がディスクに残る。ハンドルを書き込みキューの外へ
  > 持ち出さずに済むため状態の共有も生じない。

- [x] **T15: オフライン検知器** ✅ 2026-07-25
  - `OfflineDetectorParameters` / `SwingCandidate` / `DetectedImpact` / `OfflineSwingDetector`
  - パラメータ: 閾値（既定 3.0g）・不感時間（既定 0.4秒）・ピーク探索幅・角速度ゲート
  - 候補位置は閾値超過点ではなく**その後 150ms 内の加速度最大サンプル**に寄せる。
    超過点は真のインパクトよりわずかに早いため（F-I8-6 の微調整幅を減らす）
  - チャンク単位で読み捨て、不感時間だけを引き継ぐ。1セッション68万サンプル・46MB を
    同時に保持できないため
  - 検知器は状態を持たない純関数のため、パラメータを変えて何度でも走らせ直せる
  - テスト10件 ✅（0.6秒間隔の連続ショットを取りこぼさない回帰テストを含む）

  > Why not Watch 側 `SwingDetector` を流用する: 別ターゲットであり同期グループの構成上
  > 共有には project.pbxproj の手編集が要る（T1 と同じ理由）。加えて責務が異なる。
  > Watch 版は窓を切り出して保存するが、オフライン版は候補位置だけを返し、
  > 窓の切り出しは書き出し時に行う（F-I8-9）。

  > Why not 窓長を検知パラメータにする（計画時の想定）: 連続記録では窓の切り出しが
  > 検知から分離されるため、窓長は検知の挙動に影響しない。書き出し時の
  > パラメータ（T16e）へ移した。代わりに Watch 実装で窓長と一体化していた
  > 不感時間を独立パラメータとして外に出した。

- [x] **T16a: アノテーションのデータモデルと永続化**（F-I8-8/10）✅ 2026-07-25
  - `SessionAnnotation` / `AnnotatedEvent` / `AnnotationStatus` / `EventOrigin`（Domain）
    - 選別・一括付与・手動追加・位置調整・時刻オフセットはすべて純粋な mutating 操作
    - 時刻オフセットは各イベントを書き換えず補正値として保持し、書き出し時に適用する。
      書き換えると元の検知位置が失われ、別の値で試し直せない
  - `AnnotationStore`（Infrastructure）: `Documents/annotations/{sessionId}.json` へ
    操作のたびに保存。取り消し用に操作前の状態を20件まで保持
  - `merge(detected:)` が再検知でも人の判断を失わない。0.25秒以内の候補を同一とみなし、
    承認・却下・手動追加・微調整済みの位置を残す。消えるのは未レビューの検知候補のみ
  - 日付は**小数秒つき** ISO8601 で保存。既定の `.iso8601` は秒で切り捨てるため、
    5ms 粒度の位置調整（F-I8-6）が永続化で失われる
  - テスト: Domain 17件 ✅ / Store 9件 ✅

  > Why not 却下を削除する: 却下は「検知器がここで誤って発火した」という負例であり
  > （F-I8-10）、削除すると同じ誤検知を何度も却下し直すことになる。

- [x] **T16b: 同期再生の共通基盤**（F-I8-4）✅ 2026-07-25
  - `ContinuousTimeline`（Domain）: 対象範囲を引数で受け、壁時計 ⇄ セグメント/オフセットを
    解決する。欠落区間（`TimelineGap`）の列挙と、境界をまたぐ再生のための
    `nextCoveredDate(atOrAfter:)` を持つ
  - `WaveformDownsampler` / `TimedSample` / `WaveformBin`（Domain）: 区間内の**最大値**で間引く
  - `WaveformLoader`（Infrastructure）: 対象範囲に重なるチャンクだけを読む。
    チャンクの範囲は次チャンクのアンカーから推定する（終端はファイル末尾にしか無い）
  - `SyncPlaybackController`（Presentation）: **位置の基準を壁時計1つに統一**。
    動画の再生位置も波形スクラブもこの時刻へ変換するため双方向に追従する。
    セグメント境界では `AVPlayerItem` を差し替える
  - `ContinuousWaveformView` / `SyncPlayerView`（Presentation）: 両画面が共有するビュー。
    範囲の違いはコントローラへ渡すタイムラインだけで、ビューは範囲を意識しない
  - `VideoStore.existingSegmentURL` を追加（ディレクトリを作らない読み取り専用の問い合わせ）
  - テスト: タイムライン14件 ✅ / 間引き6件 ✅

  > Why not 画面ごとに再生制御を持つ: 同期の挙動が食い違うと、選別画面で確認した位置が
  > タイムライン画面で違って見える。対象範囲だけを引数で受け、制御は1つに保つ。

  > Why not 中断区間へのシークを弾く: 「動画が無い」のか「打っていない」のかを
  > 区別できないと見落とし探しが成立しない（F-I8-4）。位置としては受け付け、
  > 動画が無いことを画面で示す。再生開始時のみ次に動画がある位置へ送る。

  以下は当初の計画（達成済み）。
  - 波形と動画の双方向同期。波形スクラブで動画が追従し、動画再生で波形が追従する
  - 対象範囲を引数で受ける（選別＝候補の前後数秒 / タイムライン＝セッション全体）。
    **範囲の違いだけで作り分けないこと**。挙動が食い違うと、選別画面で確認した位置が
    タイムライン画面で違って見えることになる
  - セグメントをまたぐシーク: `impactAt` から対象セグメントと内部オフセットを解決し、
    境界では `AVPlayerItem` を切り替える。中断区間に入った場合の表示も定める
  - 波形描画は表示倍率に応じて間引く。**平均ではなく区間内の最大値**を採ること
    （1セッション約68万サンプル。ピークが平滑化で消えると見落とし発見が成立しない）
  - 既存の `VideoSyncPlayerView` を発展させて再利用する

- [x] **T16c: 選別画面**（F-I8-1/2/3）✅ 2026-07-25
  - `AnnotationReviewView`: 球種6種＋却下の7ボタンを常時表示し、**1タップで
    承認＋球種指定＋次の候補へ移動**まで完了する
  - 候補へ移動した時点で自動再生（`focusCurrentEvent`）。共通経路は操作ゼロ
  - 候補の前後は書き出しの既定窓と同じ2秒ずつ。選別時に見た範囲がそのまま
    学習データの範囲になるため、判断と結果が食い違わない
  - 一括付与は Stepper で件数指定（1〜30件）。ブロック練習に合わせる
  - 進捗・残件数・ピーク値を常時表示。取り消しはツールバー
  - 起動時に候補が空なら検知器を走らせ、以降は前回の位置から再開する（F-I8-8）
  - `AnnotationSessionListView` を「タグ付け」タブとして追加（F-I8 6章のセッション一覧）。
    連続記録のチャンク数を併記し、0本なら赤字で示す（候補が作れない原因を画面で示すため）
  - ⚠️ 実データでの動作確認は未実施（シミュレータのアクセス許可が未付与）。
    受け入れ基準「325件を20分以内」の実測も未実施

  以下は当初の計画（達成済み）。
  - 候補を順送りし、**承認＋球種指定を1操作で完了**する。2段階に分けない
  - 球種6種＋却下の計7つを常時表示。保留・取り消しも可能に
  - 連続する候補への一括タグ付け（既存 `ContentView.applyBulkTag` の発想を踏襲）
  - T16b を候補の前後数秒に適用する。**候補へ移動した時点で自動再生**し、
    共通経路は操作ゼロで判断できること。スクラブは迷う候補のための手段であり既定ではない
  - 受け入れ基準: 325件を20分以内（1件あたり平均3.7秒）

- [x] **T16d: タイムライン画面**（F-I8-5/6/7）✅ 2026-07-25
  - `AnnotationTimelineView`: T16b をセッション全体へ適用。選別画面と**同一の**
    `SyncPlaybackController` / `SyncPlayerView` を使い、渡す範囲だけが異なる
  - 表示倍率を 全体 / 5分 / 1分 / 10秒 で切り替える。倍率を狭めるときは再生位置を中心に
    置く（見つけたピークを拡大して確かめる操作のため）
  - 再生位置へのイベント追加、最寄りイベントの ±5ms 調整・再生位置へのスナップ・削除
  - 時刻オフセットはスライダーで -2〜+2秒（0.05秒刻み）。全イベントへ一律に適用
  - 欠落区間の合計秒数と区間数を要約表示
  - `WaveformLoader.loadBins` をチャンクごとの間引き＋最大値統合に変更。
    セッション全体（約68万サンプル）を同時に保持しないため
  - テスト: 統合ロジック2件を追加 ✅

  以下は当初の計画（達成済み）。
  - T16b をセッション全体に適用する。中断区間は動画の欠落として明示する
  - 波形全体を俯瞰し、**マーカーの無いピーク**として見落としを視認・補完する
  - 個別イベントの `impactAt` 微調整（5ms 粒度）
  - セッション単位の時刻オフセット一括補正

- [x] **T16e: 書き出し**（F-I8-9）✅ 2026-07-25
  - `ExportWindow` / `AnnotationExporter`（Infrastructure）+ `AnnotationExportView`
  - 窓長を書き出し時に指定する（前後 0.5〜5.0秒・0.5秒刻み。既定は前後2秒）
  - 出力は `<ShotClass>/<sessionId>_<index>.csv` + `manifest.csv`。列構成はスイング単位CSVと同一
  - 却下は `REJECTED/` へ負例として出力できる（F-I8-10）
  - manifest に窓長とサンプル数を含める。異なる窓長の出力を後から区別できるようにするため
  - テスト6件 ✅（窓長 0.5秒 → 201行 / 2.0秒 → 801行 で切り出し長が変わることを含む）

  > Why not `TrainingDataExporter` を拡張する: 既存はラベル付きCSVを**コピー**する処理で、
  > 入力が「1スイング=1ファイル」であることに依存している。連続記録では窓の切り出しが
  > 書き出し時の処理になり、入力も出力の作り方も異なる。出力形式だけを踏襲した。

  以下は当初の計画（達成済み）。
  - `confirmed` のみを対象に、連続記録から `impactAt` 中心の窓を切り出す
  - **窓長を書き出し時に指定できること**（既定 前2秒・後2秒）。
    これが連続記録方式の主要な利点であり、窓長変更に再収集も再タグ付けも不要になる
  - 出力形式は既存 `TrainingDataExporter` を踏襲

- [x] **T17: 容量の警告** ✅ 2026-07-25
  - `StorageCapacity`（Domain）: 空き容量と録画可能時間の見積り。
    1時間分（6GB）を下回ったら逼迫と判定する
  - 録画画面の上部に警告を出す。削除の提案はしない（自動削除も期限削除も行わない方針。F-I7-4）
  - 空き容量は `volumeAvailableCapacityForImportantUsage` で読む。
    `volumeAvailableCapacity` はシステムが回収できる領域を含まず実際より小さく出る
  - テスト2件 ✅

---

## 保全データ

2026-07-21 の実機検証データは吸い出し済み。**端末側の中間データ（映像）は既に削除されており復元不可。**

保全先: `~/Documents/TennisAnalyser-FieldData/2026-07-21/`（2026-07-25 退避済み）

```
pull_swings/                   # 328 CSV（2セッション分。破損0件で検証済み）
container_listing.txt          # アプリコンテナの全ファイル一覧（421件）
clip1.mov                      # 唯一生成されたクリップ（4.00秒 / 1920x1080）
analyze.py                     # メタデータ解析スクリプト
README.md                      # データの由来と内容
```

> リポジトリ外に置いている（24MB のバイナリを含むため）。
> W5 の閾値分析はこのデータで実施できる。失うと1時間の実練習をやり直すことになる。

---

## 再開手順

1. 本ファイルのチェックボックスで進捗を確認する
2. `git log --oneline -10` で直近のコミットを確認する
3. `Logs/{最新日付}.log` のネクストアクションを読む
4. ビルド確認（iOS/Watch 両方。AGENTS.md ルール5）
5. 未完了の最初のタスクから着手する

Wave の途中で中断した場合は、そのWaveの完了済みタスクまででビルドが通る状態に
してからコミットすること（AGENTS.md 方針1）。

### コンテキスト運用ルール

1セッションのコンテキストは 200k。**状態を知るための操作**が最も高くつくため、以下を守る。

| ルール | 理由 |
|---|---|
| `git diff` は必ずパス指定（`git diff -- <file>`） | パス無しの全体 diff は単発で約 8k を消費する |
| 200行超のファイルは全読みしない。`grep -n` で当たりを付けて `Read` の offset/limit | 3ファイル全読みで約 7k |
| ビルド出力は捨てる: `xcodebuild ... 2>&1 \| tail -8` | 成功時の全ログは無価値 |
| 本ファイルは該当 Wave だけ読む | 全文 180行超 |
| **Wave 完了ごとにコミットし、チェックボックスを更新する** | 未コミット状態は次セッションに全体 diff を強制する |

### サブエージェントへの委譲

委譲コスト（ブリーフィング＋報告受領）は 3〜5k。**中間生成が 10k を超えるタスクのみ**委譲する。

| 委譲する | 理由 |
|---|---|
| W5 の閾値分析 | Swift 不要・リポジトリ非依存。中間生成 15〜25k |
| 変更前の影響範囲調査（Explore） | fan-out 検索は中間が大きく結論は小さい。規約のブリーフィング不要で委譲コストが 1k 以下 |
| 実機ログ（logarchive）からの事象抽出 | 生ログは大量、結論は数行 |

| 委譲しない | 理由 |
|---|---|
| 実装そのもの | diff レビューが必須で、読むコスト ≒ 書くコスト。規約（コメント How/What/Why not・正式名称）の伝達分だけ純損 |
| ビルド確認・コミット・doc 更新 | `\| tail` 済みで数百トークン。冷えた起動の方が高い |
| Wave 間の設計判断 | 文脈が必要。冷えた状態の判断は品質リスク |

> 例外: Wave の途中でコンテキストが逼迫した場合は、残りを詳細スペック付きで委譲し
> 自分はレビューに専念する。窓を使い切って巻き戻すよりは確実に安い。

---

## 実機検証項目（W4 完了後）

仕様書「5. 受け入れ基準」に従う。検証前に以下を必ず実施すること。

1. 短時間の実機テストで中断→復帰を確認（1時間の実練習を無駄にしないため）
2. 診断記録がApp内で読めることを確認
3. **検証後、アプリを起動する前にコンテナを吸い出す**

```bash
xcrun devicectl device copy from --device <UUID> --domain-type appDataContainer \
  --domain-identifier com.spleeing.TennisAnalyser --user mobile \
  --source Documents --destination ./pull
```
